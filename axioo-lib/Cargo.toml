[package]
name = "axioo-lib"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "Read-only hardware introspection library for Axioo Pongo / Clevo laptops on Linux"

[dependencies]
libc = "0.2"
serde = { version = "1", features = ["derive"] }
toml = "1"
